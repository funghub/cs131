A1: ls -l > listing.txt && date >> listing.txt
A2: ls /nonexistent_dir 2> errors.txt
A3: ls /nonexistent_dir /etc 1> output.txt 2> errors.txt
A4: ls /nonexistent_dir /etc &> combined.txt
A5: I see the standard output because only the error is redirected. /dev/null is a device file like a black hole in which we can dispose of unwanted output
A6: sort fruits.txt | uniq
A7: sort fruits.txt | uniq -c | sort -nr
A8: grep 'ERROR' server.log | wc -l
A9: grep 'ERROR' server.log > errors_only.txt
A10: grep -oE '^[A-Z]+' server.log | sort | uniq -c | sort -nr
A11: grep -oE '"state":"[^"]+"' /data/yelp/yelp_academic_dataset_business.json | sort | uniq -c | sort -nr | head -5
B1: uid=1040(ishigaki-cs7) gid=1040(ishigaki-cs7) groups=1040(ishigaki-cs7),44(video),988(docker),993(render)
B1: uid is user id to identify me as a user. gid is to identify which group of users I am in.
B2: -rw-rw-r-- (owner -rw for read and write, no execute)(group -rw for read and write, no execute)(other r-- for read only) 
B3: 0002 is the default umask. The default permissions for new files are rw- for owner, rw- for group, and r-- for others. 
B4: chmod 000 secret.txt 
B4: Permission denied error
B4: chmod u+rw secret.txt
B5: chmod 640 fruits.txt
B6: find . -type f -user $(whoami) -perm -u=w
C1: 2003375
C2: job number 1, status Running, command bash long.sh > log.txt &
C3: Stopped
C4: bg %1
C5: Default signal is TERM
C6: ps -p 2004874
C7: disown detaches the process running from the current terminal and jobs but & simply runs the process in the background still as a job.
